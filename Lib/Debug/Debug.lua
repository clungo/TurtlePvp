-- This is named after the SuperMacro method. This is because a lot of the initial code was written as a superMacro. This should be changed at some point lol
function Printd(message)
    DEFAULT_CHAT_FRAME:AddMessage(message,1,1,1);
end